<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Home</title>
</head>
<body>
    <p>Home tab</p>
    <ul>
        <% for (let p of posts) { %>
            <li><%= p.title %> [<%= p.price %>] - <%= p.username %> <a href="edit">Details</a></li>
        <% } %>
    </ul>
    <a href="create">Create Post</a>
</body>
</html>